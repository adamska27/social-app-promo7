<!DOCTYPE html>
<html>

<head>
  <% include ../partials/head %>
    <link rel="stylesheet" href="../../css/user.css">
</head>

<body>
  <header>
    <% include ../partials/header%>
  </header>

  <section class="user-container container-fluid">
    <div class="row">
      <button type="button" class="btn btn-primary col-lg-offset-9" data-toggle="modal" data-target="#myModal">
        Modifier les informations du profil
      </button>

      <form class="col-lg-1" action="/users/<%= id %>" method="post" onsubmit="return confirm('Êtves-vous sur de vouloir supprimer ce profil?')";>
      <button type="submit" class="btn btn-danger">
        supprimer le profil
      </button>
      </form>

      <div class="jumbotron">
        <p>Vous êtes sur le profil de
          <%= apprenant[0].firstname %>
          <%= apprenant[0].lastname %>
        </p>
      </div>

      <div class="container-img-profil">
        <!-- <img class="img-profil" src="<%= apprenant.img %>" alt="#"> -->
      </div>
    <p>  <a href="<%= apprenant[0].id %>/projects" />Voir ces projets</a></p>
    </div>
  </section>

  <div style="text-align: center">
    <div class="modal fade" id="myModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
      <div class="modal-dialog" role="document">
        <div class="modal-content">
          <form action="/users/<%= id %>/modif" method="post">
            <div class="modal-header">
              <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
              <h4 class="modal-title" id="myModalLabel">Mise à jour du profil</h4>
            </div>
            <div class="modal-body">
              <div>
                    <label for="">Votre prénom : </label>
                    <div class="input-group input-group-lg col-xs-12">
                      <input type="text" class="form-control" aria-describedby="sizing-addon1" name="firstname" maxlength="25" placeholder="<%= apprenant[0].firstname %>" required>
                    </div>

                    <label for="">Votre nom : </label>
                    <div class="input-group input-group-lg col-xs-12">
                      <input type="text" class="form-control" placeholder="<%= apprenant[0].lastname %>" aria-describedby="basic-addon2" name="lastname" maxlength="25" required>
                    </div>

                    <label for="">Votre pseudo Github : </label>
                    <div class="input-group input-group-lg col-xs-12">
                      <input type="text" class="form-control" placeholder="<%= apprenant[0].pseudoGithub %>" aria-describedby="basic-addon3" maxlength="35" required>
                    </div>

                    <label for="">Your image URL</label>
                    <div class="input-group input-group-lg col-xs-12">
                      <span class="input-group-addon" id="basic-addon4">url : </span>
                      <input type="url" class="form-control" id="basic-url" aria-describedby="basic-addon4" maxlength="200" placeholder="https://example.com/img/">
                    </div>
              </div>
            </div>
            <div class="modal-footer">
              <button type="button" class="btn btn-default" data-dismiss="modal">Fermer</button>
              <button type="submit" class="btn btn-primary">Sauvegarder les changements</button>
            </div>
          </form>
        </div>
      </div>
    </div>
  </div>

  <footer>
    <% include ../partials/footer %>
  </footer>

</body>
</html>
